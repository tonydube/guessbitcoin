import { Box, Typography } from "@mui/material";

const Settings = () => {
  return (
    <Box
      sx={{
        maxWidth: 600,
      }}
    >
      <Typography variant="h4" gutterBottom>
        Settings
      </Typography>
    </Box>
  );
};

export default Settings;
